%h1 Listing routines

%table
  %tr
    %th Type
    %th
    %th
    %th

  - @routines.each do |routine|
    %tr
      %td= routine.type
      %td= link_to 'Show', routine
      %td= link_to 'Edit', edit_routine_path(routine)
      %td= link_to 'Destroy', routine, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Routine', new_routine_path
